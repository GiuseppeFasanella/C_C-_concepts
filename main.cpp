#include<iostream>

int main() {
  std::cout<<"ciao pippo!"<<'\n';
  return 0;
}

//compile it with g++ -o prova2 pippo.cpp
//g++ actually calls gcc with some options to let it know that it's a C++ code
